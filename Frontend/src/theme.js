// src/theme.js
import { createTheme } from '@mui/material/styles';
 
const theme = createTheme({
  palette: {
    primary: {
      main: '#ff6f61', // Change this to your desired primary color
    },
    secondary: {
      main: '#ffdbd3', // Change this to your desired secondary color
    },
  },
});
 
export default theme;